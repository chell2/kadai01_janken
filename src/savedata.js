// function saveBarsData(userId, url, number, location) {
//   const sheet = SpreadsheetApp.openById(SPREADSHEET_ID).getSheetByName(bars);
//   const now = new Date();
//   sheet.appendRow([userId, url, number, location, now]);
// }
